#define _CRT_SECURE_NO_WARNINGS 
#include<stdio.h>
#include<Windows.h>
/*
int f(int i, int j, int(*a)[4])
{
	int f1, f2, tmp = 0, k;
	if (i == 0 || j == 0)
		return a[0][0];
	if (j == i)
	{
		for (k = 0; k <= i; k++)
			tmp += a[k][k];
		return tmp;
	}
	f1 = f(i - 1, j, a);
	f2 = f(i - 1, j - 1, a);
	if (f1 < f2)
		return f2 + a[i][j];
	else
		return f1 + a[i][j];
}

int main()
{

	system("pause");
	return 0;
}
*/

void printcom(char* a, char* b)
{


}




int main()
{

	system("pause");
	return 0;
}