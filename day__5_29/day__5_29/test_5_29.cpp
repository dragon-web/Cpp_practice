#define _CRT_SECURE_NO_WARNINGS 

#include<stdio.h>
#include<windows.h>
int main()
{
	/*int arr[10] = { 0 };
	int max = arr[0];
	for (int i = 0; i < 10; ++i)
	{
		scanf("%d", &arr[i]);
		if (arr[i] > max)
		max = arr[i];
	}
	printf("%d\n", max);


	int count = 1;
	int num = 0;
	int max = num;  
	while (scanf("%d", &num),count <= 10)
	{
		max = max > num ? max : num;
		count++;
	}
	printf("%d", max);
	system("pause");
	return 0;
}
*/



